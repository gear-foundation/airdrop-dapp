import { Container } from '@/components'
import { Button } from '@gear-js/vara-ui'
import React from 'react'

export const Home = () => {
	return (
		<Container>
			<Button text='Button' />
		</Container>
	)
}
